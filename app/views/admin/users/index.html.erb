<h1>All Users</h1>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= user.first_name %>
        <%= button_to "Remove #{user.first_name}", admin_user_path(user), method: :delete, action: "Destroy" %>
  </li>
  <% end %>
</ul>
